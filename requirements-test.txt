# ─────────────────────────────────────────────
# Daly BMS Interface — dépendances de test
# Usage : pip install -r requirements-test.txt
# ─────────────────────────────────────────────

-r requirements.txt

# --- Framework de test ---
pytest>=8.0.0
pytest-asyncio>=0.23.0         # tests async (asyncio mode = auto)
anyio[trio]>=4.0.0             # backend async pour pytest-asyncio

# --- Tests API ---
httpx>=0.27.0                  # client HTTP pour TestClient FastAPI

# --- Tests MQTT (intégration) ---
aiomqtt>=2.0.0                 # roundtrip MQTT avec Mosquitto local
